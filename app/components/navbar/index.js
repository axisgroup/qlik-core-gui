import React from 'react';
import logo from '../../../resources/logo.png';

export default () => (
  <div>
    <img
      src={logo}
      style={{
        height: 30,
        marginLeft: 30,
        marginRight: 40,
        marginTop: 15
      }}
      alt=""
    />
  </div>
);
